# Electron 二进制不走 GitHub，走 npmmirror。
# 本机 npm 已配 registry=https://registry.npmmirror.com，但 Electron 的**二进制**
# 默认从 GitHub 取，npm install 会「成功」却不装 dist/electron.exe。
# 这一行让 @electron/get 走镜像（等价于设置 ELECTRON_MIRROR 环境变量）。
electron_mirror=https://npmmirror.com/mirrors/electron/
