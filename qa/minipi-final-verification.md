# minipi · 开工前终验报告

> 测试工程师：秦戈　·　日期：2026-09-22　·　性质：**终验（只验三块，不重跑历史全量）**
> 被测对象：
> 1. `docs/minipi-plan.md`（方案 v0.3，614 行）
> 2. `docs/minipi-audit.md`（审计 v2，638 行，作为核对基准）
> 3. `prototype/minipi-prototype.html`（原型 v0.5，2425 行）
> 4. `spike/electron-window/`（窗口物理 spike 套件）
> 5. `spike/pi-sdk/`（Pi SDK 运行时 spike 套件）
> 6. `vendor/pi/packages/coding-agent/src/`（SDK 源码，只读核对）
>
> 环境：Windows（OS 10.0.22631）· Node **v22.22.2** · Chrome（headless=new）+ CDP · 报告只写本文件。
> **本次未改动任何被测文件**；测试脚手架位于 `%TEMP%\minipi_qa\`（自建）。

---

## 〇、执行摘要

| 层面 | 用例数 | 通过 | 失败 | 说明 |
|---|---|---|---|---|
| 任务一（方案自洽） | 60 | 55 | 5 | A1 闭合 30 条（2 条有残留）、A2 落位 11 条全过、A3 新矛盾 4 条、A4 源码抽样 14 条全过、A5 扫描见下 |
| 任务二 · 功能 | 51 | 51 | 0 | 真实 Chromium + 真实鼠标/键盘事件 |
| 任务二 · 边界 | 10 | 10 | 0 | 分辨率 2 档 ×2 + 超长输入 + 空/加载/错误态 |
| 任务二 · 抽查回归 | 25 | 25 | 0 | 抽查率 25/45 ≈ 56% |
| 任务三（spike 静态） | 2 套件 | 2 | 0 | 两套件自检 EXIT=0、0 失败；node --check 全通过 |
| **合计（可判定）** | **148** | **143** | **5** | 失败项全部为**方案文档残留/行号漂移**，无功能缺陷 |

**缺陷分级汇总**：P0 = **0**　·　P1 = **0**　·　P2 = **1**　·　P3 = **5**。

> 安全层面（越权 / IDOR / 参数校验 / 401 / 403）**不适用于本次对象**：原型是**纯静态 HTML 文档**（无后端、无接口、无鉴权面），方案 v0.3 本阶段也未产出可运行服务。任务三的两个 spike 均为**本地离线工具**，无对外服务面。此项**如实记为不适用**，不以「基本覆盖」含糊带过。

---

## 一、任务一 · 方案 v0.3 的闭合度与自洽性

### A1 · P0 / C / N 逐条闭合核对（证据＝`docs/minipi-plan.md` 行号）

#### A1.1 三个 P0

| 编号 | 结论 | 证据（plan.md 行号） |
|---|---|---|
| **P0-1** 闸门位置改为官方 `pi.on("tool_call")` | **已关闭** | §3.3:196（方向变更 P0-1，自建包装层删除）；§3.3:200/202/204/205/206（钩子声明/事件/可变 input/返回值/内联扩展入口）；§3.3:208-238（`gateFactory` 用 `pi.on("tool_call")` 实现）；§3.3:243-245（官方用法与 `ctx.hasUI` 论证） |
| **P0-2** 宿主 pin 受支持线 `44.x`、删「LTS 线」误述 | **已关闭** | §7.1:433（「pin 到当前受支持线 `44.x`（例 `44.4.3`）」）；§7.1:435（「官方**没有 LTS 线**——策略是支持最新 3 个稳定大版本」）；§11:596（门禁序 2 已关闭） |
| **P0-3** Node ≥ 22.19 + 主进程纯 ESM | **已关闭** | §11:585（Node `>= 22.19.0`，来源 `package.json:104-106`）；§11:586（纯 ESM / `exports` 只有 `import` / 主进程必须 ESM）；§8:464（M0-0 基线） |

#### A1.2 C1–C19（C18 为审计自撤的误报，不计）

| 编号 | 结论 | 证据（plan.md 行号） |
|---|---|---|
| C1 热键主次唯一口径 | 已关闭 | §0:24（「唯一口径」；已删「最高频入口」定性词） |
| C2 抢焦点默认值 | 已关闭 | §0:322 + §10:535（D-01） |
| C3 桥接/窗口形态 | 已关闭 | §2:60（方向变更 C3/C4：单窗口三态） |
| C4 Full 态 frameless + 自绘标题栏 | 已关闭 | §2:67 + §4.1:299/301/302 |
| C5 会话转正（钉住） | 已关闭 | §3.2:184-192（5 步 + 失败回滚） |
| C6 study 不含 edit | 已关闭 | §3.2:182 |
| C7 审批端点组 | 已关闭 | §3.1:144-147（approvals 三条端点） |
| C8 会话列表端点 | 已关闭 | §3.1:135（`GET /api/sessions`） |
| C9 判定层不阻塞主流程 | 已关闭 | §6:371 + §6.3:413（「不阻塞主流程，但影响通知策略（在呈现路径上）」） |
| C10 逐字保留决策 + 阈值边界 | 已关闭 | §6.2:401（`>= 0.5`）+ §6.3:418（统一 `>=`） |
| C11 焦点归还粒度 | 已关闭 | §0:21 + §10:537（D-02） |
| C12 桥接唯一通道 | 已关闭 | §2:59（无 sidecar / 无端口文件）+ §7.2:456（只走 ws） |
| C13 目录根统一 | 已关闭 | §3.2:176（统一 `~/.minipi/`） |
| C14 快捷键记法 | 已关闭 | §6.2:405（删 `ui.hint`/⌘K）+ §10:564（D-11）；全文无 ⌘K |
| C15 M5 时间预算 | 已关闭 | §8:496-499（拆 M5a/M5b） |
| C16 `prompt` 出参 | 已关闭 | §3:119 + §3.1:138（无 `queuePosition`） |
| C17 预览自算 | 已关闭 | §3.3:264（`buildPreview` 由 params 自算） |
| **C18** | **误报，无需处理** | 审计 §四:479 自撤（QA 裁定误报），v0.3 未据此改任何内容 |
| C19 删「已无待决问题」 | 已关闭 | §11:579/603（唯一未关闭项＝序 7）；全文无「已无待决」 |

#### A1.3 N1–N9

| 编号 | 结论 | 证据（plan.md 行号） |
|---|---|---|
| N1 §5 表统一「七级」 | 已关闭 | §5:338-357（0–6 七行）+ §8:485；全文无「六级」 |
| N2 等审批暂停计时规则 | 已关闭 | §5:359-363（三条计时规则） |
| N3 贴边/吸附/调宽降级 | 已关闭 | §4.1:298 + §10:560（D-09） |
| N4 Node ≥ 22.19 | 已关闭 | §11:585 |
| N5 主进程 ESM | 已关闭 | §11:586 |
| **N6** 术语「速问」统一为 `speed` | **有条件关闭（有残留）** | 见 A3 · **V2**：§6:371「速问分支」、§10:571「速问」快路径仍残留 |
| **N7** Full 态 `Esc` 行为 | 已关闭 | §4.1:289（状态机）+ §4.1:299 + §8:477（M1 验收） |
| **N8** 删「问完自动收起」对策 | **有条件关闭（有残留）** | §9:510-524 已移除该对策；但 §0:13、§4.1:292 残留承诺 → 见 A3 · **V1** |
| N9 删「工作区选择器」入口 | 已关闭 | §6.2:405 + §10:564（D-11） |

> **A1 小结**：P0-1/2/3 全部关闭；C1–C19（除 C18）全部关闭；N1–N5/N7/N9 关闭，**N6、N8 各有文档级残留**（详见 A3）。

### A2 · D-01~D-11 是否逐条写入「已确认的决定」节，且与审计推荐项一致

基准：`docs/minipi-audit.md` §九 决策表（:594-604，全部「推荐 A」）；落位：`docs/minipi-plan.md` §10（:529-573）。

| 编号 | 审计推荐 | plan §10 行号 | plan 采用的选项 | 是否一致 |
|---|---|---|---|---|
| D-01 | A｜默认抢焦点 | §10:535 | 默认抢焦点，可开「不抢焦点」；判据「不把原 App 切到后台」 | ✅ 一致 |
| D-02 | A｜只验窗口级 | §10:537 | caret 恢复 best-effort，不计入验收 | ✅ 一致 |
| D-03 | A｜接受 Electron 常驻 | §10:539 | 接受 300–500MB，Tauri 后置 | ✅ 一致 |
| D-04 | A｜不设 Windows 下限 | §10:541 | 不设限，仅记录 Win11 22000/DWM 前提 | ✅ 一致 |
| D-05 | A｜固定三动作 | §10:546 | 允许一次/拒绝/中断整轮；「本会话总是允许」降为设置项、默认关 | ✅ 一致 |
| D-06 | A｜命令级边界 | §10:548 | 命令级 + 完整原文 + 审计日志 | ✅ 一致 |
| D-07 | A｜接受出境并明示 | §10:553 | 接受，首次启动明示 | ✅ 一致 |
| D-08 | A｜不加鉴权 | §10:555 | 不加，注明共享机器需另议 | ✅ 一致 |
| D-09 | A｜本期只做拖动+位置记忆 | §10:560 | 本期不做贴边/吸附/调宽 | ✅ 一致 |
| D-10 | A｜本期不做 | §10:562 | 「问完自动收起」本期不做 | ✅ 一致 |
| D-11 | A｜本期不做 | §10:564 | 「工作区选择器」本期不做 | ✅ 一致 |

> **A2 小结**：D-01~D-11 **全部**写入 §10 已确认的决定节，**逐条与审计推荐项（全 A）一致**，无遗漏、无偏离。

### A3 · 新引入的前后不一致（V 清单）

| 编号 | 描述 | 证据（plan.md 行号） | 级别 |
|---|---|---|---|
| **V1** | **「问完自动收起」残留承诺**：§0 定位句仍写「问完就收起」，§4.1 状态机图仍写「问完 → 静默球 + 未读小圆点」，与 D-10「本期不做」冲突。审计 N8 明确要求「§9 移除该承诺」并指出 §4.1 那句「亦无验收」——§9 已移除，但 §0/§4.1 未同步 | §0:13「问完就收起，不切窗口」；§4.1:292「问完 → 静默球 + 未读小圆点（不弹窗）」；对照 §8:504、§10:562（D-10 本期不做） | **P2** |
| **V2** | **术语「速问」未完全统一为 `speed`**（审计 N6 要求统一，仅 v0.1→v0.2 变更说明处可保留） | §6:371「因为**速问**分支已砍掉」；§10:571「不设「**速问**」快路径」；对照 §7:7（历史行，允许保留） | P3 |
| **V3** | **SDK 断言行号引用漂移**（内容正确、行号错） | ① §3.3:206 `resource-loader.ts:168, 254, 957` → 254 未命中，实际 **268**；② §3:121 `agent-session.ts:3584`（cancelled 返回）→ 实际 **3598**；③ §3:119 `agent-session.ts:273-274` → 273 未命中，实际 **274** | P3 |
| **V4** | **门禁序 7 归属口径未随 spike 拆分更新**：§11 把「窗口物理 + `navigateTree`/`terminate`/`reason`」合并列为**归属＝宿主**的一次 spike；但实际已交付**两套**（`spike/electron-window`＝宿主物理 / `spike/pi-sdk`＝SDK 运行时），后者明确覆盖 `navigateTree`/`terminate`/`reason` | §11:601（归属「宿主」）；对照 `spike/pi-sdk/README.md:15-17`、`spike/electron-window/README.md:161` | P3 |

> 已排除的「疑似矛盾」（经核对**不成立**）：§2 架构图（:63-83，单窗口三态）与 §4.3（:324-330）**已同步**；§3.1 `queue_update`（:120/156-157）与源码 `agent-session.ts:173-175` **一致**；§5 无「六级」残留；§3.3「中断整轮」与 `terminate` 聚合语义（:247）**与源码一致**；sidecar/端口文件**无残留**（§2:59/77 明确否认）；D-09/D-11 在 §4.1/§6.2/§8/§9 **无残留**（仅 D-10 有 V1）。

### A4 · SDK 断言回源码抽样（`vendor/pi/packages/coding-agent/src/`）

抽样 **14 处**，**全部与源码一致**（无一处断言为假）。

| # | plan 断言 | 断言处 | 源码实证（文件:行号） | 结论 |
|---|---|---|---|---|
| 1 | `engines.node >= 22.19.0` | §11:585 | `package.json:104-106`（`"engines":{"node":">=22.19.0"}`） | 一致 |
| 2 | `"type":"module"`、`exports` 只有 `import` | §11:586 | `package.json:5`（`type`）· `:14-18`（`exports` 仅 `import`+`types`） | 一致 |
| 3 | 凭证默认目录 `~/.pi/agent` | §11:587 | `core/sdk.ts:44`（注释 `Default: ~/.pi/agent`）· `:180`（`authPath`） | 一致 |
| 4 | `ToolCallEventBase = { type:"tool_call"; toolCallId }` | §3.3:203 | `core/extensions/types.ts:973-976` | 一致 |
| 5 | `ToolCallEvent` 联合 + 各子类带 `toolName`/`input` | §3.3:203 | `types.ts:978-1016`（8 工具子类）· `:1029-1038`（联合）· `:1018-1021`（Custom） | 一致 |
| 6 | `event.input` 可变 | §3.3:204 | `types.ts:1026-1027`（`is mutable. Mutate it in place…`） | 一致 |
| 7 | `ToolCallEventResult = { block?; reason?; terminate? }` | §3.3:205 | `types.ts:1217-1226` | 一致 |
| 8 | `terminate` 需**同批每个** finalized 结果为 true | §3.3:247 | `types.ts:1221-1225`（注释）· `agent-loop.ts:685-686`（`every(...terminate===true)`） | 一致 |
| 9 | 官方钩子 `on("tool_call",…)` 返回 `() => void` | §3.3:202 | `types.ts:1416` | 一致 |
| 10 | 内置 8 工具名 | §3.2:178 | `types.ts:978-1016`（bash/powershell/read/edit/write/grep/find/ls） | 一致 |
| 11 | `preflightResult` 签名 `(success:boolean)=>void`，Internal | §3:119 | `agent-session.ts:274`（签名）· `:1759`（`preflightResult?.(true)`）· `:1751`（false） | 一致 |
| 12 | `queue_update` 字段 `{steering,followUp}` | §3:120 | `agent-session.ts:173-175` | 一致 |
| 13 | 流式中缺 `streamingBehavior` 抛错 | §3:117 | `agent-session.ts:1655-1663` | 一致 |
| 14 | `SessionManager.create(cwd,dir)` / `inMemory(cwd?)` | §3.2:189 | `core/session-manager.ts:1752` · `:1801` | 一致 |

> **A4 结论**：抽查 14 处（>10），**无一处断言对不上** → **不触发 P1**。仅 V3 列出的 3 处**行号引用**漂移（内容正确），计 P3。

### A5 · 无出处断言 / 「未验证写成已验证」扫描

| 项 | 结果 | 证据 |
|---|---|---|
| 「社区出于省电…」 | **已清除** | `grep 社区\|省电` in plan → 命中 0 |
| 「E27+ 白闪」 | **已清除** | `grep E27` in plan → 命中 0 |
| 「Electron 14+ `roundedCorners`」 | **已清除** | `grep Electron 14` in plan → 命中 0；改述为 D-04（§10:541）的「Win11 Build 22000」前提 |
| 「已无待决问题」 | **已清除** | `grep 已无待决` → 命中 0（C19） |
| 「把未验证写成已验证」 | **未发现** | §9:525 明确「结论口径只用三种」；§11:601 序 7 标「待 spike」；§3.3:246 对 `reason` 回灌标「未在源码中找到」 |
| 无内联出处的平台断言（残留） | **2 处** | §7.1:435「Electron 无 LTS 线 / 支持最新 3 个稳定大版本」、§11:586「Electron 28+ 才支持 ESM 主进程」——均为**公认事实**，但 plan 未给出处链接 | 

> **A5 结论**：v0.3 已把 v1/v2 点名的**无出处断言全部清除**；`grep` 复核 0 命中。残留 2 处 Electron 平台断言为公认事实但**缺内联出处**，计 **P3**（建议补官方链接，不影响开工）。

---

## 二、任务二 · 原型 v0.5 回归（真实 Chromium + CDP）

### B1 · 复跑上轮关键项

| 项 | 结果 | 实测证据 |
|---|---|---|
| **P1-4 形态切换不重放（长度单调不减）** | **PASS（已修复）** | `T15`：形态切换序列 `[54,100,135,135,135,135]` —— 单调不减且末值>首值；`setMode()` 不重建 `#miniBody`，`state.streamText` 为唯一真源（原型 `:1735-1795`） |
| **第二次提问重置流** | PASS | `T13`：firstLen=116 → secondLen=2（重置不回显）；`T14`：气泡＝`pretest_第二次提问` |
| **球右键菜单 + Esc 后视觉真关闭** | PASS | `T05` display:block,w=194 → `T06` display:none,w=0,`data-menu=closed` → `T07` hover 不重弹 → `T08` 点空白关闭 |
| **审批卡四张按钮集合** | PASS | `T10`：edit/write/bash/batch **四张**均为 `["允许一次","拒绝","中断整轮"]` |
| **中断整轮真中断** | PASS | `T11`：lv3→点 abort→lv=6、抽屉关闭、toast「中断整轮（aborted）…传播给同批其余 pending」（`hit.inside=true, occluded=false`）；`T12`：计时器变「未启动」、球复位 `idle`、`#streamOut` 消失 |
| **study 权限文案无 edit** | PASS | `T17` 工具行＝`read · write · grep · find · ls`（无 edit）；`T18` 权限行＝`可写 —— write 必须逐次确认（study 无 edit / bash）` |
| **错误态无端口文件** | PASS | `T19` `hasPortFile=false,hasSidecar=false`；`T20` 文案＝「连接失败：bridge 未响应（主进程未就绪 / 会话初始化失败）」 |
| **1280×800 / 1366×768 七级全可见可点** | PASS | `R-1280x800`/`R-1366x768`：`bad=[]`，且真点第 6 级生效（`level=6`） |
| **控制台报错 0** | PASS | `F1`：`count=0`；`B02`：超长输入后新增报错 0 |
| **自包含性** | PASS | `S1-S4`：`link=0,scriptSrc=0,@import=0,@font-face=0,fetch=0,xhr=0,ws=0,beacon=0,ES=0,img=0,srcset=0,data-uri=0`；外链仅 1 条 `https://example.invalid/install.sh`（示例占位）；`F2`：运行时外部请求 0 |

### B2 · 本轮新增 5 项（必须真验）

| 新增项 | 结果 | 实测证据 |
|---|---|---|
| ① 等审批期间 8s 计时器暂停 | PASS | `N-01`：进入第 3 级后状态行＝「已暂停」（进入前＝「计时中 剩余 7.2s」）；`N-02`：等审批 >11s 仍 lv=3、未切大窗 |
| ② 本轮内手动收起过则不再自动升级 | PASS | `N-03`：点「收起」后状态行＝「已停用」；`N-04`：>11s 仍 lv=1、未切大窗（真实跑满 11s 等待） |
| ③ D-09/D-10/D-11 文案改「本期不做（已拍板）」 | PASS | `N-05/06/07`：均命中「本期不做（已拍板 D-XX）」；`N-08`：全文「尚未实现」仅 **1** 处且在源文件注释（第 15 行），非 UI 残留 |
| ④ C4 相关 Windows 透明窗备注 | PASS | `N-09`：含「不能用系统菜单、也不能双击标题栏最大化」＋「`maximize()` API 待 spike」；`N-10`：含「全形态 frameless + 自绘标题栏」 |
| ⑤ §3.3 机制改为官方 `tool_call` 钩子 | PASS | `N-11`：抽屉 note＝「用 Pi 官方的 `pi.on("tool_call")` 前置钩子…」；`N-12`：全文无「工具注册层」残留（=0） |

### B3 · 抽查上轮通过项（防回归）

抽查 **X01–X25 共 25 条**，**全部 PASS**（抽查率 25/45 ≈ 56%，远超「≥10 条」要求）。覆盖：Enter 发送、Shift+Enter 换行、Esc 收起、Alt+Space 开合、代码块复制、级别↔球态联动（unread/await）、待确认跨形态保持、顶部横幅、工具行展开、三场景工具集、钉住/用项目打开、会话列表切换、思考等级、附件入口、大窗内联审批卡、合并卡勾选、拒绝/允许语义、开关默认关。另 `B01/B02`（4000 字符超长输入）PASS。

> **任务二小结**：**75 个用例 75 PASS / 0 FAIL / 0 INFO**（`final_v05.json` 的 `summary={"total":75,"pass":75,"fail":0,"info":0}`）。上轮的两处失败（P1-4、球右键菜单）**均已修复**。

---

## 三、任务三 · 两个 spike 套件可用性（静态，未真跑 GUI / 未跑 live）

### C1 · `spike/electron-window/`

| 检查项 | 结果 | 证据 |
|---|---|---|
| Electron 版本受支持线 + 精确 pin | 通过 | 自检项「electron 精确 pin 44.x → `44.4.3`」；`package.json` engines `>=22.19.0`、`type:module` |
| `type:module` | 通过 | 自检项「package.json type=module」 |
| `node --check` 全部 `.mjs` | 通过 | `main.mjs` / `lib/pixels.mjs` / `lib/util.mjs` / `scripts/selfcheck.mjs` 均 `OK` |
| 自检（41 项） | 通过 | `node scripts/selfcheck.mjs`：**共 41 项检查，0 失败**，`EXIT=0` |
| README / RESULT-TEMPLATE 可判定 | 通过 | README 给 T1–T4 逐项「怎么判读」表；RESULT-TEMPLATE 给「报告字段 + 判定（✅/❌/⚠️/🤔）」逐行可填 |
| 四角 alpha 检测逻辑 | 通过（逻辑正确） | `lib/pixels.mjs:7-53`：`toBitmap()` 按 **BGRA** 取像素、`a=bmp[i+3]`；`cornerVerdict` 判据 `allCornersTransparent = maxCornerAlpha===0`；`main.mjs:122-123/233` 调用并记录 `maxCornerAlphaAcrossSteps`；另设 `methodControl.alphaSensitive` 方法自检与 `desktopCapturer` 品红背板独立核对（`:272-313`） |
| `.gitignore` 覆盖 `out/` | 通过 | `.gitignore` = `node_modules/` + `out/` |
| 产物是否含敏感信息 | 无产物 | 目录内**无 `out/`**（spike 未真跑），源码自检确认「无真实路径 / 无凭证 / 无邮箱」 |

### C2 · `spike/pi-sdk/`

| 检查项 | 结果 | 证据 |
|---|---|---|
| `spike:offline` 与 `spike:live` 两入口存在且可跑 | 通过 | `package.json` scripts：`spike:offline=node src/offline.mjs`、`spike:live=node src/live.mjs`、`spike:live:plan=…--plan`、`selfcheck=…` |
| offline **真跑一遍** | 通过 | 实跑 `npm run spike:offline` → **`EXIT=0`**，重新生成 `out/pi-sdk-offline-report.{json,md}`；结论速览 B1 import 成功 / B2 缺 `getSessionsDir` / B3 19/23 完全命中 / B4 provider 1 个 `deepseek` |
| live 默认不自动执行 | 通过 | 自检项「live.mjs 需 `--run`」＋「需 `MINIPI_SPIKE_LIVE_CONFIRM=1`」＋「`--max-model-calls` 默认上限 10」；README §1.2 明确默认只打印计划 |
| 输出目录被 `.gitignore` 覆盖 | 通过 | `.gitignore` = `node_modules/` + `out/`；自检项「.gitignore 忽略 out/」通过 |
| 脚本无泄漏凭证 | 通过 | 自检项「未发现真实路径 / 未发现凭证 / 未发现邮箱」通过；`offline.mjs` 只读 `auth.json` 的**键名**，不读值 |
| 自检（9 项 + 扩展项） | 通过 | `node src/selfcheck.mjs`：**共 9 项检查，0 失败**，`EXIT=0` |
| `node --check` 全部 `.mjs` | 通过 | `offline.mjs` / `live.mjs` / `selfcheck.mjs` / `lib/scan.mjs` / `lib/static-facts.mjs` / `lib/util.mjs` 均 `OK` |
| 产物是否含敏感信息 | **未检出** | `out/pi-sdk-offline-report.md` §5 仅输出「文件是否存在 + provider 名称 + **字段名**（`type`/`key`）」，**无任何凭证值、无绝对路径**（落盘字符串统一过 `redact()`） |
| 报告自身准确性 | **1 处误报** | F1「行号漂移」表声称 `toolCallId: string;` 实际行＝429、文档写 975；**实测 `types.ts:975` 即为 `toolCallId: string;`**（`ToolCallEventBase` 内），429 是 `ToolRenderContext` 里的同名行 → 该「漂移」为**探针误报**（详见缺陷 P3-5） |

> **任务三小结**：两套件**结构完整、可跑、可判定**；`selfcheck` 与 `node --check` 全绿；offline 真跑 `EXIT=0`；**未发现凭证泄漏**。

---

## 四、缺陷清单

> 说明：本次所有可判定失败项**均为方案文档级问题**，无功能缺陷、无安全缺陷。按「不要把所有问题都报成 P0」的口径如实分级。

### P2-1 · 方案残留「问完自动收起」承诺，与已拍板的 D-10 冲突

- **描述**：plan v0.3 在 §0 定位句与 §4.1 状态机图仍把「问完自动收起」写作既有行为，而 D-10 已拍板「本期不做」，形成残留承诺。
- **复现步骤**：① 打开 `docs/minipi-plan.md`；② 读 §0 第 13 行「问完就收起，不切窗口」；③ 读 §4.1 第 292 行「问完 → 静默球 + 未读小圆点（不弹窗）」；④ 对照 §8 第 504 行与 §10 第 562 行（D-10「本期不做」）。
- **实际结果**：§0:13、§4.1:292 仍承诺「问完自动收起」；审计 N8 证据栏明确指出 §4.1 那句「亦无验收」。
- **预期结果**：§0 定位句改为「按 `Esc` 收起」；§4.1 状态机删去「问完 → 静默球」自动迁移，或显式标注「本期不做（D-10）」。
- **影响范围**：M3/M6 验收——评审者会把它当成既有能力，验收无从判定（正是审计 N8 判「承诺无排期」的原因）。
- **证据**：`minipi-plan.md:13`、`:292` vs `:504`、`:562`。
- **定位**：`docs/minipi-plan.md:13`、`docs/minipi-plan.md:292`。
- **修复建议**：按审计 N8/D-10 的 A 选项，把两处改为「用户按 `Esc` 收起；自动收起重启与否不在本期范围」。

### P3-2 · 术语「速问」未完全统一为 `speed`

- **描述**：审计 N6（低）要求统一为 `speed`（仅历史变更说明处保留「速问」），v0.3 仍有 2 处非历史语境残留。
- **复现步骤**：`grep 速问 docs/minipi-plan.md`。
- **实际结果**：命中 `:7`（历史行，允许）、`:371`（§6 正文）、`:571`（§10 决定）。
- **预期结果**：仅 `:7` 保留；`:371`、`:571` 改为 `speed`。
- **影响范围**：文档一致性，读者可能误判为两个概念。
- **证据**：`minipi-plan.md:371`、`:571`。
- **定位**：`docs/minipi-plan.md:371`、`docs/minipi-plan.md:571`。
- **修复建议**：`:371` 改「因为 `speed` 分支已砍掉」；`:571` 改「不设 `speed` 快路径」。

### P3-3 · SDK 断言行号引用漂移（内容正确）

- **描述**：3 处 `文件:行号` 引用与源码不符（断言内容本身正确）。
- **复现步骤**：回 `vendor/pi/packages/coding-agent/src/` 定位对应行。
- **实际结果**：① `resource-loader.ts` 的 `extensionFactories` 赋值在 **268**，plan 写 254；② `agent-session.ts` 的 `return { cancelled: false }` 在 **3598**，plan 写 3584；③ `preflightResult` 签名在 **274**，plan 写 273-274（273 未命中）。
- **预期结果**：改引用行号。
- **影响范围**：仅文档可追溯性；`pi-sdk` 离线报告 F6/F17 已同步列出。
- **证据**：`pi-sdk/out/pi-sdk-offline-report.md` F6「行号漂移」表（3585→3510）、F17 说明（254 未命中）。
- **定位**：`docs/minipi-plan.md:206`、`:121`、`:119`。
- **修复建议**：按 `pi-sdk` 离线报告 §4 的「行号漂移」表逐条修引用。

### P3-4 · 门禁序 7 归属口径未随 spike 拆分更新

- **描述**：§11 把「窗口物理 + SDK 语义」合并为**一次宿主 spike**，但实际已拆成两套件。
- **复现步骤**：读 plan §11 第 601 行「归属＝宿主」；读 `spike/pi-sdk/README.md` 与 `spike/electron-window/README.md`。
- **实际结果**：宿主物理归 `electron-window`；`navigateTree`/`terminate`/`reason` 归 `pi-sdk`（`electron-window/README.md:161` 明确「本套件刻意不碰」）。
- **预期结果**：§11 序 7 的「归属」列改为「宿主 + SDK 运行时（两套件）」，并列明各套件产物路径。
- **影响范围**：里程碑排期与门禁判定口径。
- **证据**：`minipi-plan.md:601` vs `spike/pi-sdk/README.md:15-17`、`spike/electron-window/README.md:161`。
- **定位**：`docs/minipi-plan.md:601`。
- **修复建议**：更新序 7 描述，指向两套件的 `RESULT-TEMPLATE.md`。

### P3-5 · `pi-sdk` 离线报告 F1「行号漂移」为误报

- **描述**：`out/pi-sdk-offline-report.md` F1 行号漂移表把 `toolCallId: string;` 的「实际行」标为 429（称文档写 975 有误），但源码 `types.ts:975` 正确即为该行。
- **复现步骤**：读 `types.ts:973-976`（`ToolCallEventBase`）；再读报告 F1 漂移表。
- **实际结果**：报告称真实行 429（`ToolRenderContext.args` 附近的同名行）；实测 `:975` 正确。
- **预期结果**：F1 漂移表应判定「无漂移」（或标注为探针在重复行上取首个命中导致的误报）。
- **影响范围**：会误导方案作者改掉**本来正确**的行号。
- **证据**：`vendor/pi/.../extensions/types.ts:975`（`\ttoolCallId: string;`）vs 报告 F1 漂移表（429 / 975 / -546）。
- **定位**：`spike/pi-sdk/src/lib/scan.mjs`（行匹配策略）→ 产物 `spike/pi-sdk/out/pi-sdk-offline-report.md` F1。
- **修复建议**：探针命中改为「优先校验期望行号所在行内容」，重复文本不作为漂移依据。

### P3-6 · 2 处 Electron 平台断言无内联出处

- **描述**：§7.1:435「Electron 无 LTS 线、支持最新 3 个稳定大版本」与 §11:586「Electron 28+ 才支持 ESM 主进程」为公认事实，但未给出处链接。
- **复现步骤**：`grep -n "LTS\|Electron 28" docs/minipi-plan.md`。
- **实际结果**：两处均无引用来源。
- **预期结果**：附 Electron 官方文档链接（或标注「需以官方 release 说明复核」）。
- **影响范围**：低——属公认事实，且 §11 门禁序 7 会以 spike 实测窗口物理。
- **证据**：`minipi-plan.md:435`、`:586`。
- **定位**：`docs/minipi-plan.md:435`、`docs/minipi-plan.md:586`。
- **修复建议**：补官方链接。

---

## 五、未验证项（如实列出，未测到即写「未验证」）

- **`spike/electron-window` 四项实测（T1 透明 setBounds / T2 `maximize()` / T3 未聚焦节流 / T4 运行期 `setBackgroundThrottling`）**：**未验证**。按任务要求「静态，不真跑 GUI」，本次只验套件可用性与检测逻辑，**未启动 Electron**。→ 需在目标机 `npm install && npm start` 真跑。
- **`spike/pi-sdk` 的 `spike:live`（Q1–Q5）**：**未执行**。需真实模型、消耗额度，默认不自动执行。
- **Electron 主进程 ESM 的**运行期**验证**：**未验证**（纯 Node 下只能验 `import`；Electron ESM 主进程需真跑）。
- **原型中「未聚焦窗口不刷新」「真实全局热键 / 焦点归还 / 系统级透明」**：**未验证**——原型是浏览器文档，无法复现 WebView 节流与原生窗口行为（原型自身 `:787` 亦如此声明）。
- **并发 / 多用户 / 越权**：**不适用**——静态原型无后端、无鉴权面，本次无可测的攻击面。

---

## 六、测试数据与清理

- 测试输入统一使用 `pretest_` 前缀（如 `pretest_第一次提问`、`pretest_回车发送`），**未写入任何被测产物**。
- 脚手架与临时产物全部位于 `C:\Users\yang6\AppData\Local\Temp\minipi_qa\`（自建）：`final_v05.js`、`final_v05.json`、`final_v05.txt`、`page.js`、`cdp.js` 及截图；以及 Chrome 临时 profile `prof_final_v05_*`。
- **被测目录未新增/修改任何文件**；`spike/pi-sdk/out/` 为 `npm run spike:offline` 真跑时按设计重新生成（已被该套件 `.gitignore` 覆盖）。
- 清理状态：脚手架保留在 `%TEMP%`（属本机临时目录，可随时删）；**未触碰** `docs/`、`prototype/`、`spike/` 下源码。

---

## 七、五大结论（一句话）

### ① 任务一（方案 v0.3 闭合度与自洽性）
**基本达标**：P0-1/P0-2/P0-3、C1–C19（除 C18 误报）、N1–N5/N7/N9 **全部关闭**（P0-1 见 §3.3:196-238；P0-2 见 §7.1:433/435；P0-3 见 §11:585/586）；D-01~D-11 **全部写入 §10 且与审计推荐逐条一致**；A4 抽查 14 处 SDK 断言**全部对得上**；A5 无出处断言已清除。**唯一实质残留 = V1（「问完自动收起」）**，另有 3 处行号/术语/口径级 P3。

### ② 任务二（原型 v0.5 回归）
**全绿**：**75 用例 75 PASS / 0 FAIL**；上轮两处失败（形态切换重放 P1-4、球右键菜单 P2-2）**均已修复**；本轮新增 5 项（等审批暂停计时、手动收起不升级、D-09/10/11 文案、C4 Windows 备注、§3.3 官方钩子表述）**全部真验通过**；抽查 25/45 条防回归全过；控制台报错 0、外部请求 0、自包含成立。

### ③ 任务三（两个 spike 套件）
**可用**：`electron-window` 与 `pi-sdk` 两套件结构完整、`selfcheck` 全绿、`node --check` 全通过；`pi-sdk` 的 `spike:offline` **真跑 EXIT=0**、`spike:live` 默认不执行；两套件 `.gitignore` 均覆盖 `out/`；**未发现凭证泄漏**。唯一瑕疵：`pi-sdk` 离线报告 F1 行号漂移为误报（P3-5）。

### ④ 缺陷清单
**P0 = 0　P1 = 0　P2 = 1（P2-1）　P3 = 5（P3-2 ~ P3-6）**。全部为**方案文档级**问题，**无功能缺陷、无安全缺陷**。

### ⑤ 交付分档判定
- **M0（骨架：ESM 主进程 + SDK 接线 + 未聚焦节流）**：**可以开工**。前提在本机已实测满足——Node **v22.22.2 ≥ 22.19.0**；`import` SDK 成功；`~/.pi/agent/auth.json` 存在且含 1 个 provider（`deepseek`）。**但 M0-3（未聚焦节流）的真实效果仍属「未验证」**，建议 M0 内用真窗口实测（或跑 `spike/electron-window` 的 T3/T4）。
- **M1（单窗口三态变形）**：**不可开工，须先关闭门禁序 7**。序 7 的 **SDK 半边**已由 `spike:offline` 真跑关掉；但**窗口物理半边（透明 `setBounds` 是否掉透明 / `maximize()` 是否可用 / 变形耗时 / 白闪 / 未聚焦节流）尚未真跑**——**必须**先在目标机跑 `spike/electron-window` 拿到 T1 结论后，M1 才能启动。
- **一句话**：**M0 可开工，M1 待 spike（窗口物理）**；方案 v0.3 建议顺手修掉 P2-1 与 P3-2~P3-6 六条文档级问题后即达「可交付」状态。

---

*报告结束。所有命令输出、响应内容、实测数值均见上文代码块与证据列；未实测项已逐条标注「未验证」。*
